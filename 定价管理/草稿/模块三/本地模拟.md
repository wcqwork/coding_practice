### 写入es失败

### 本地模拟
文件：PhoenixFormMessageServiceImpl.java
// updateEsMessage(inquiryReferSource);  // 本地临时跳过 ES
log.info("[ReferSource] msgId={}, comId={}, referSource={}", messageId, comId, inquiryReferSource != null ? inquiryReferSource.getReferSource() : "null");



### 详细日志：
从日志来看，进展很好！之前的 SmartDataAssociationFeignService 错误已经解决了（感谢你启动了 SmartDataRunnerApplication）。

但现在卡在了下一步 —— 更新 Elasticsearch 数据时失败了：

PhoenixFormMessageEsServiceImpl.updateReferSource
java.net.ConnectException: Connection timed out: no further information
Elasticsearch 服务在你的本地环境不可达（ES 一般部署在服务器上），日志中也能看到 ES 健康检查同样超时了：

Elasticsearch health check failed
java.net.ConnectException: Connection timed out: no further information
处理流程走到哪一步了
步骤        状态
1. 扫描 USE_STATUS=2 的待处理记录
成功
2. 通过 SmartDataAssociationFeignService 获取站点数据
成功（启动了 SmartDataRunner 后）
3. 处理 comId 的询盘来源数据
进入处理
4. 更新 ES 中的询盘消息来源标记
失败 - ES 连接超时
因为 ES 更新失败，异常被捕获后 USE_STATUS 保持为 2 待重试。

结论
这已经是本地环境的极限了。 ES 是基础设施服务，本地无法连接。整个代码逻辑链路已经验证到最后一步：

订单消费 -> USE_STATUS=2 设置 -- 已验证
Job 扫描 -> 找到待处理数据 -- 已验证
Dubbo 服务调用 -> SmartData 获取数据 -- 已验证
ES 更新 -> 本地无法完成（部署到 beta 后 ES 可用，这步就通了）
可以放心部署到 beta 环境测试了，beta 上 ES、所有微服务都是就绪的，完整流程可以跑通，USE_STATUS 最终会从 2 变为 0。